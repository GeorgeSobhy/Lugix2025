@model TagModel

@{
    ViewData["Title"] = "Add New Tag";
}

<style>
    body {
        background-color: #4070f4;
    }

    .tag-form-container {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }

    .tag-form-body {
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: #4070f4;
    }

    .tag-form-wrapper {
        background: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 500px;
        margin-top: 10%;
    }

    .tag-form-group {
        margin-bottom: 15px;
    }

    .tag-form-label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
    }

    .tag-form-input {
        width: 100%;
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 4px;
    }

    .tag-form-error {
        color: red;
        font-size: 12px;
    }

    .tag-form-check-label {
        margin-left: 5px;
    }

    .tag-form-submit-button {
        background-color: #4070f4;
        border: none;
        padding: 10px 20px;
        color: white;
        border-radius: 4px;
        cursor: pointer;
    }

        .tag-form-submit-button:hover {
            background-color: #3056d3;
        }

    .tag-form-back-link {
        display: block;
        margin-top: 15px;
        text-align: center;
        color: #4070f4;
        text-decoration: none;
    }

        .tag-form-back-link:hover {
            text-decoration: underline;
        }
</style>

<div class="tag-form-body">
    <div class="tag-form-wrapper">
        <h4>Add New Tag</h4>
        <hr />
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="tag-form-error"></div>

            <div class="tag-form-group">
                <label asp-for="Name" class="tag-form-label"></label>
                <input asp-for="Name" class="tag-form-input" />
                <span asp-validation-for="Name" class="tag-form-error"></span>
            </div>

            <div class="tag-form-group">
                <label asp-for="Description" class="tag-form-label"></label>
                <input asp-for="Description" class="tag-form-input" />
                <span asp-validation-for="Description" class="tag-form-error"></span>
            </div>

            <div class="tag-form-group">
                <input type="submit" value="Create" class="tag-form-submit-button" />
            </div>
        </form>

        <div>
            <a asp-action="Index" class="tag-form-back-link">Back to List</a>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}